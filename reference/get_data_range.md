# Get data range

This function extracts rows and column indices to slice the .full_sheet
into metabolites, conc_values and meta_data.

## Usage

``` r
get_data_range(full_sheet)
```

## Arguments

- full_sheet:

  full_sheet
